{
    "autoload": {
        "psr-4": {
            "AcmeWidget\\": "src/"
        }
    },
    "require-dev": {
       "phpunit/phpunit": "^10.0",
        "phpstan/phpstan": "^2.1"
    },
    "scripts": {
        "test": "phpunit tests/",
        "phpstan": "phpstan analyse"
    },
    "config": {
        "platform": {
            "php": "8.2.0"
        }
    }
}